package spells

/*
Nexus spells module.
*/

func AddSpell(newSpell Spell) {
	return
}

func(* Spells) spellsByName(){
	return
}

func PopulateSpells(){
	return
}

/* Handle a spell cast
def handle_effect(self, effect, self, caller, target):

# Just apply the effect to the self, caller
if(SPELL_EFFECTS['action'] == 'apply'):
test = "value"
return True

# Execute the effect function of the spell
elif(SPELL_EFFECTS['action'] == 'execute'):
runeffect = getattr(self, effect.replace('-', '_'))
runeffect(self, caller, target)
return True

elif(SPELL_EFFECT['action'] == 'combat'):
return True
*/